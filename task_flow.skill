# Skill: Task Flow for Task Notebook App

请严格按业务闭环实现流程。

## 业务主流程

### 阶段 1：创建明天计划
用户在今天为“明天”创建事项计划。
创建内容包括：
- 多条事项 title
- 每条事项可选 note

创建后生成一个 PlanDay：
- targetDate = 明天日期
- 每条事项 status = todo

### 阶段 2：执行当天计划
当到达 targetDate 对应日期时，用户需要逐项处理：
- 做完：点打勾
- 没做：点打叉

### 阶段 3：计划闭环
当某个计划日下所有事项都不再是 todo 时：
- 该计划日状态改为 finished
- 系统允许创建下一天计划

### 阶段 4：下一天计划解锁
只有最近计划日 status = finished 时，才可以新增后一天计划

## 约束逻辑
1. 最近计划日如果还有 todo：
   - 禁止创建新计划
2. 最近计划日如果已经 finished：
   - 允许创建新计划
3. 同一日期不允许重复创建计划
4. 用户打开应用时，默认优先展示最近一个未闭环的计划日
5. 若历史计划全部闭环，则引导创建下一天计划

## 异常场景处理
1. 没有任何计划日时：
   - 显示空状态
   - 引导创建第一天计划
2. 某日没有事项：
   - 默认不允许保存空计划
3. 事项标题为空：
   - 阻止提交
4. 重复点击打勾/打叉：
   - 应保持幂等或做状态保护
5. 修改事项状态后，统计数据应实时更新
